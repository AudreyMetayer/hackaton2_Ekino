<nav class="navbar navbar-expand-lg">
    <div>
    <a href="{{ path('home_index') }}">
        <img class="mt-4 ml-3 navbar-brand"src="{{ asset('build/images/daylink.svg') }}" width="80px" class="d-inline-block align-top" alt="">
    </a>
    </div>
    <div>
        <a   href="{{ path('app_login') }}">
            <img class="mt-3 mr-3" src="{{ asset('build/images/user.png') }}"  width="60px" alt="">
        </a>
    </div>
</nav>